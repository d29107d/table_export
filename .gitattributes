# 统一按 LF 存库、按 LF 检出，避免 Windows(Git for Windows 默认 autocrlf=true)
# 把源码在磁盘上反复改写成 CRLF，制造无意义的 diff
* text=auto eol=lf

# 二进制，别做任何转换
*.xlsx binary
*.xls  binary
*.exe  binary
*.png  binary
*.ico  binary
*.jpg  binary

# 示例导出产物：是"工具的输出",要按原样保存。
# 导出器固定写 CRLF，若被规范化成 LF，重新生成时整份文件都会被标成已修改。
example/**/*.lua -text
